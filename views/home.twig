{% extends 'layout.twig' %}

{% block content %}
	{% for post in posts %}
		<div class="post-listing">
			<h1>{{ post.title }}</h1>
			<div class="content">{{ post.content }}</div>
		</div>
	{% endfor %}
{% endblock content %}
