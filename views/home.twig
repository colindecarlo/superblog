{% extends 'layout.twig' %}

{% block content %}
	{% for post in posts %}
		<div class="post-listing">
			<h1><a href="/read/{{ post.post_id }}">{{ post.title }}</a></h1>
			<div class="content">{{ post.content }}</div>
		</div>
	{% endfor %}
{% endblock content %}
